# 改善内容（2026-09-03）

このZIPは、ChatGPT Sitesから書き出した受付システムをベースに、既存仕様を維持したまま安全性・待ち時間予測・通信負荷を中心に改善したものです。

## 変更点

- **定員超過をサーバー側でも防止**
  - 直接入場は現在人数だけでなく「案内中グループのために確保すべき席」も考慮します。
  - 案内中グループの入場確定時にも、その時点の定員を再確認します。
  - 受付UIでも、直接入場可能な空き人数を超える場合はボタンを無効化します。
- **待ち時間予測を実際の案内優先度に合わせる**
  - 従来の整理券番号順の単純シミュレーションではなく、`P = グループ人数 + 待ち時間 / 基準回転時間` と空き確保ルールを使って予測します。
  - 案内中グループの席も予測上予約済みとして扱います。
- **利用者向け整理券ページの更新を安定化**
  - 遅い通信で2秒更新が重複しないようにしました。
  - 画面が非表示の間は不要な定期通信を止め、復帰時に即更新します。
  - 古い応答が新しい表示を上書きしないようにしました。
  - 「あなたまで○組」は案内優先度と一致しないため、「受付順で前に○組」と明確化しました。
- **状態取得を軽量化**
  - 通常の受付状態取得で不要だったSNSリンク取得を省略しました。
  - 利用者向け状態取得では不要な操作履歴取得を省略しました。
  - 受付状態で使わない統計内訳列をアクティブグループ取得から外しました。
- **操作IDをイベント履歴に引き継ぐ**
  - クライアントの `requestId` を可能な範囲でイベントの `op_id` として利用し、操作単位の追跡・取り消しを分かりやすくしました。
- **例外処理を補強**
  - 手動呼び出し時に当日状態が取得できなかった場合の明示的なエラー処理を追加しました。
- **案内ロジックのテストを追加**
  - 優先度順の待ち時間予測と、案内中グループの席予約を確認するテストケースを追加しました。

## DBについて

今回の改善では新しいテーブルや列を追加していないため、**追加のD1マイグレーションは不要**です。

## 確認

- 変更したTypeScript/TSXファイルはTypeScriptの構文変換でエラーがないことを確認済みです。
- 案内優先度・待ち時間予測の主要ケースは単体スモークテストを通しています。
